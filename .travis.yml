language: node_js

node_js:
  - 10

branches:
  only:
    - master
    - next
    - /^greenkeeper.*$/

jobs:
  include:
    - if: branch = next
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
